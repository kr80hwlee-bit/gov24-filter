# EVAL-3 — docs/PLAN.md 독립 채점 (3회차)

채점 대상: `/Users/ttaeng-guli80/gov24-filter/docs/PLAN.md` @ `16c3ec726a31b56446385e0f8a9fe6574f2def38`(`git rev-parse HEAD`, 워킹트리 clean 확인)
루브릭: `/Users/ttaeng-guli80/gov24-filter/docs/RUBRIC.md`(고정본)
채점자: rubric-evaluator 서브에이전트(3회차 독립 인스턴스, 1·2회차 채점자·작성자·REVIEW-1 검수자 모두와 분리)
1·2회차 대비: EVAL-1 86.9 FAIL → EVAL-2 94.0 FAIL(전 항목≥90, 평균 미달) → 이번 회차 재검. 커밋 로그: `d1232d9`(초기 구현) → `16c3ec7`(REVIEW-1 N등급 4건 수정 + EVAL-2 4건 반영)

## 독립 재현 실행 로그 (본 채점자, 3회차)
- `python3 -m unittest discover -s tests -p "test_*.py"` → `Ran 37 tests … OK`, rc=0 (EVIDENCE E-7 "37 tests"와 일치, EVAL-2 시점의 35에서 회귀 테스트 2건 증가)
- `node --test tests/filter.test.mjs` → `# pass 26 / # fail 0`, rc=0 (EVIDENCE E-7 "26"과 일치, REVIEW-1이 지적한 미커밋 테스트 파일 문제 해소 — `git status --short` clean)
- `bash scripts/verify_no_key.sh` → `OK`(.env 부재로 1단계 건너뜀 안내), rc=0
- `DATA_GO_KR_SERVICE_KEY=INVALIDKEYTEST123 python3 scripts/check_key.py; echo rc=$?` → 세 방식 모두 HTTP 401 `{"code":-4}` → `FAIL` + "원인: 미등록 키 — … 활용신청 현황에 15113968 이 있는지 확인 …" · rc=1 (AC-2 기대 출력과 문자 그대로 일치)
- `curl https://api.odcloud.kr/api/gov24/v1/serviceList?...` → HTTP 404 `{"code":-3,"msg":"등록되지 않은 서비스 입니다."}` — PLAN L38 "v1은 2026-09-02 실측 HTTP 404 {"code":-3}" 재확인
- `curl https://api.odcloud.kr/api/gov24/v3/serviceList (키 없음)` → HTTP 401 `{"code":-401,"msg":"인증키는 필수 항목 입니다."}` — PLAN L43 "키 누락 -401" 재확인
- `curl https://infuser.odcloud.kr/api/stages/44436/api-docs` 재조회 → `serviceDetail_model` 필드에 정확히 `행정규칙 자치법규 법령 공무원확인구비서류 본인확인필요구비서류` 포함 — PLAN L45·EVIDENCE E-1 재확인(2회차와 동일 결과, API 드리프트 없음)
- `grep -c` 로 Manyfast ID 38종 전수 대조(`for id in ...`) → 전부 RTM(§4)에 1회 이상 등장, MISSING 0건
- `python3 -c "math.ceil"` 재계산: 10,929~10,999건→66회, 11,001건→69회, ×4(최악 재시도)=264~276회/10,000=2.64~2.8% → PLAN L72 "66~69회…0.7%", R-8(L218) "2.8%"와 일치
- `sed -n '224,247p' | grep '^|' | wc -l` → 24행(헤더+구분선 2줄 제외 22행) — §8.1 "Manyfast 미결 질문 22건" 재확인
- `grep -rn 'odcloud\|serviceKey' public/*.js public/*.html public/*.css` → 0건, `grep -c 'class="disclaimer' public/index.html` → 4 — AC-8·§3 키비노출 주장 재확인
- 코드 대조: `scripts/fetch_snapshot.py:132-137`(count_check 정수 캐스팅) · `:274-278`(diff_snapshots None/빈문자열 정규화) · `public/app.js:74`(safeHref, http/https만 허용) — REVIEW-1 N등급 4건 중 코드 수정 3건 전부 실재·동작 확인
- research 원문 대조: `research_oss_deploy.md` A-2 표(라인 95 "perPage=100" bid-collectors / 라인 109 sole-search "perPage=500") → PLAN L41 "sole-search 실사용 … bid-collectors는 perPage=100 사용" 표기가 정확함(2회차 지적 해소)

## 2회차 지적 4건 해소 여부 (줄 번호 확인)
| # | 2회차 지적 | 해소 여부 | 근거(줄) |
|---|---|---|---|
| 1 | L41 perPage=500 근거에 bid-collectors 병기(부정확) | 해소 | PLAN L41 "perPage=500 동작 `[확인]`(sole-search 실사용 … ; bid-collectors는 perPage=100 사용)" — 두 저장소를 분리해 정확한 값을 병기. `research_oss_deploy.md` A-2 재대조로 정확성 확인 |
| 2 | §9.2에 reviewer 독립 재현 대상 AC·완료여부 표 없음 | 해소 | PLAN L263-283, AC-1~15 전체를 "작성자 실행/채점자 독립 재현(EVAL-2)/검수자 독립 재현(REVIEW-1)" 3열로 표기. 각 셀의 서술이 REVIEW-1 §1·§2 내용과 대조해 정확함(예: AC-3 셀 "문자열 totalCount 결함 적발" = REVIEW-1 §2.2 count_check 결함과 일치) |
| 3 | T5b `gh repo create` 재실행 안전성이 HOWTO에만 있고 PLAN 본문엔 없음 | 해소 | PLAN L197, T5b 셀 자체에 "먼저 `gh repo view`로 존재 여부 확인 … 이미 있으면 remote 추가 후 main 업로드만 … 이미 켜져 있으면 422를 정상으로 본다 … 두 명령 모두 재실행해도 안전하다 `[결정]`" 전문 포함 |
| 4 | §1.1 "재현" 표기가 작성자 실행/채점자 재현을 구분 안 함 | 해소 | PLAN L17-19, P1/P2/P3 각 행에 "작성자 실행 = E-5·E-6(...) / 채점자 독립 재현 = EVAL-2(...) / 검수자 독립 재현 = §9.2 표(REVIEW-1)"로 3주체 명시 분리 |

## 항목별 판정

| # | 항목 | 점수 | 근거(줄) | 미달 사유 | 시정 지시 |
|---|---|---|---|---|---|
| 1 | 목표·범위 | 97 | L11(한문장 목표) · L15-20(§1.1, 3주체 검증 구분표) · L22-27(범위밖 5개 긍정형) | 사소함: L17-19의 "미완" 표기는 정직하나, AC-4·9·10·11·15 등 다수 항목이 여전히 "작성자 단독 확인"(L283)에 머물러 P1/P2 "구현 완료" 단정과 실제 독립검증 범위 사이에 체감차가 남음(용어 자체는 정확) | 없음(현행 유지, 표기 신뢰수준 유지 확인) |
| 2 | 데이터 출처·API 규격 근거 | 98 | L38(Base URL, v1 HTTP 404 재확인) · L41(perPage 정정, `research_oss_deploy.md` A-2 재대조로 정확) · L43(오류코드 -401/-3 재현 일치) · L45(serviceDetail 3필드, `curl` 재조회로 재확인, API 드리프트 없음) | 결함 없음. "공식 최대치 `[데이터공백]`"(L41)처럼 모르는 값은 정직하게 공백 처리됨 | 없음(유지) |
| 3 | 아키텍처 적합성 | 97 | L58-70(다이어그램) · L72(66~69회·0.7%, `python3` 재계산 일치) · L73-75(키 비노출 경로, `grep` 0건 재확인) | 결함 없음 | 없음(유지) |
| 4 | 요구사항 추적성 | 98 | L83-112(RTM) — 정본 38개 ID 전수 `grep -c` 재대조, MISSING 0건. L106·L108 제외 명시 | 결함 없음 | 없음(유지) |
| 5 | 함정 대응 설계 | 96 | L118-138 — 4개 함정 필드·판정값·검증방법. `resolve_region`/`classify_deadline` REVIEW-1 §2.2 표본 6+4종 전부 규칙과 일치(재대조) | 사소함: §5.2(L126) "377 대 105"를 "원문 설명이 없어 일반 규칙으로 흡수"라 명시했으나 그 일반 규칙이 원 사례를 실제로 해결하는지는 실물 데이터 없이는 검증 불가(`[추정]` 태그로 정직하게 처리돼 있어 중대 결함 아님) | 없음(현행 태그 유지로 충분) |
| 6 | 수용기준 검증가능성 | 95 | L146-162(AC-1~15 EARS+재현) · AC-2 재현 출력이 기대 문자열과 정확히 일치(rc=1) | L162/L281: AC-15 스펙은 "375px"인데 §9.2 표(L281)·EVIDENCE E-6 실측은 "371px"에서 수행돼 스펙 수치와 실측 수치가 정확히 일치하지 않음(더 좁은 폭에서 통과했으므로 위험은 낮으나 근거 수치 자체는 스펙과 다름) | AC-15 재현을 정확히 375px로 재측정하거나, 스펙 수치를 371px로 정정할 것 |
| 7 | 실행 계획 구체성 | 97 | L189-201(T0~T9) · L197(T5b idempotency 본문 내재화, 2회차 지적 해소) · L192(픽스처 경로) · `yaml.safe_load` 통과(E-2) | 결함 없음 | 없음(유지) |
| 8 | 리스크·가정·데이터공백 | 96 | L209-247(R-1~R-10, §8.1) — `sed`+`grep`로 22행 재확인(2회차와 동일) | 결함 없음. R-6(L216) "gzip 후 1MB 안팎"이 `[추정]` 태그로 남아 있어 근거 없는 확정 주장이 아님 | 없음(유지) |
| 9 | 검증·오류점검 계획 | 97 | L253-283(9.1~9.4) — §9.2 표(L267-281)가 AC-1~15 전체에 작성자/채점자/검수자 재현 현황을 명시(2회차 지적 해소). `verify_no_key.sh` 코드 대조 결과 §9.3(L285-288) 서술과 정확히 일치 | 사소함: §9.2 표는 "무엇이 아직 미검증인지"는 명확하나, 미검증 항목(AC-4·9·10·11·15)을 언제·누가 해소할지의 다음 단계(예: 브라우저 있는 세션 배정)가 문서 안에 없음 | §9.2 표 아래에 "브라우저 재현 담당·시점" 한 줄을 추가할 것 |
| 10 | 비개발자 실행 가능성 | 97 | L295-304 — 6단계에 전제조건 문구(§7.2 T5b·`gh auth status`) 포함(2회차 해소 유지). 본인만 가능한 단계 굵게 표기 일치 | 결함 없음 | 없음(유지) |

## 자동 반려 검사
RUBRIC.md에 별도 자동 반려 조항 없음. 통과선("모든 항목≥90 AND 평균≥95")으로 검사.

## 총점 계산 (Bash 실측)
```
scores = [97, 98, 97, 98, 96, 95, 97, 96, 97, 97]
sum = 968
평균 = 96.8
```

총점: 96.8 / 100
판정: **PASS** (전 항목 ≥90 이고 평균 96.8 ≥ 95)

## 2회차 대비 변화
- 94.0 FAIL → 96.8 PASS (+2.8). EVAL-2의 "다음 라운드에서 반드시 고칠 것" 4건(L41 perPage 정정 · §9.2 reviewer 재현 표 · T5b idempotency 본문화 · §1.1 검증주체 3분리) 전부 줄 번호 대조로 해소 확인.
- REVIEW-1의 N등급 4건(count_check 타입 · diff_snapshots None/"" · href 스킴 · 재현성/미커밋)도 코드·git 상태 대조로 전부 해소 확인(본 채점 범위는 아니나 §9.2 표 근거로 재확인됨).
- 새로 발견한 잔여 사소 결함 1건(AC-15 375px 스펙 vs 371px 실측)은 통과선에 영향 없는 수준.

## 가장 큰 감점 3건
1. 항목 6 — AC-15 스펙 수치(375px)와 §9.2·EVIDENCE E-6 실측 수치(371px)가 정확히 일치하지 않음. 근거 자체가 스펙과 다른 값을 인용.
2. 항목 9 — §9.2 표가 미검증 AC(4·9·10·11·15)를 나열은 하지만 해소 담당·시점이 문서에 없어 다음 액션이 불명확.
3. 항목 1/5 — 다수 AC가 여전히 "작성자 단독 확인"에 머물러 있고(L283), §5.2의 "377 대 105" 원 사례 자체는 실물 데이터 없이 검증되지 않음. 둘 다 정직하게 태그·서술돼 있어 중대 결함은 아니나 잔여 리스크로 남음.

## 다음 라운드에서 반드시 고칠 것
1. AC-15를 정확히 375px에서 재현하거나(브라우저 세션 필요), 스펙 문구를 실측 폭(371px)에 맞춰 정정한다.
2. §9.2 표에 미검증 AC(4·9·10·11·15)의 해소 담당·예정 시점을 한 줄씩 추가한다.
3. 브라우저 필요 AC(4·7·9·10·11·15)를 검수자가 실제로 재현할 세션을 배정하고 §9.2 "검수자 독립 재현" 열의 "브라우저 없음, 미검증"을 실측 결과로 교체한다.

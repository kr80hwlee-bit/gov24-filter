# EVAL-1 — docs/PLAN.md 독립 채점

채점 대상: `/Users/ttaeng-guli80/gov24-filter/docs/PLAN.md` @ 워킹트리 — 재현 불가(레포 미초기화, `.git` 없음). 대신 내용 고정용 해시로 대체: `sha256:56cec106fc82b066d4b906f601b06df35e0fbc1cc518926e23b9275c1509723e` (2026-09-02 22:53:32 mtime, 채점 시각 2026-09-02T13:59:08Z UTC)
루브릭: `/Users/ttaeng-guli80/gov24-filter/docs/RUBRIC.md` @ `sha256:6ba7148034feb9e1cf6505fed02db9c5d70edb4bd00395f3064c8d7d705db413` (버전 표기 없음 — "사전 공개본 · 고정" 단일본)
채점자: rubric-evaluator 서브에이전트(작성자 cla와 분리), 참고 리서치 2종 대조 완료

## 항목별 판정

| # | 항목 | 배점 | 점수 | 근거(줄) | 미달 사유 | 시정 지시 |
|---|---|---|---|---|---|---|
| 1 | 목표·범위 | 100 | 75 | L11(목표 한문장 양호) / L15-20(§1.1 단계표, P1·P2·P3(서버불필요) 모두 "완성" 표기) / L22-27(범위밖 긍정형 양호) | §1.1 표가 P1/P2/P3 전 단계를 "완성"으로 표기했으나 실제 저장소엔 `scripts/check_key.py` 1개 파일만 존재(`fetch_snapshot.py`·`public/`·`tests/`·`.github/` 전부 부재, `find` 실측). 완료 여부에 `[확인]/[추정]` 태그 없이 단정한 사실 주장 → 근거없는 주장 -15 규정 적용 | §1.1 "이번 세션" 열을 "완성"에서 "설계 완료·미구현"으로 정정하고, 실제 구현된 파일만 "완성"으로 표기할 것 |
| 2 | 데이터 출처·API 규격 근거 | 100 | 82 | L38(Base URL, v1 404 확인 — 리서치 일치) / L42(응답 봉투 — oss 보고서 A-0 일치) / L44(serviceList 21필드 — oss 보고서 A-0과 정확히 일치, `grep` 대조 완료) / L45("행정규칙 자치법규 법령" 포함 serviceDetail 추가필드) / L47-48(사용자구분·소관기관유형 값 — oss 보고서 A-1과 정확히 일치) | L45의 "행정규칙 자치법규 법령" 3개 필드는 `[확인]` Swagger serviceDetail model로 표기됐으나, 제공된 리서치 원문 2건(`research_api_spec.md`, `research_oss_deploy.md`) 어디에도 이 세 단어가 등장하지 않음(`grep` 0건 확인). 태그와 실제 근거 불일치 → -15 | L45에서 "행정규칙 자치법규 법령"을 제거하거나, Swagger 원문을 직접 재조회해 URL과 함께 인용하고 그 전까지 `[데이터공백]`으로 표기할 것 |
| 3 | 아키텍처 적합성 | 100 | 77 | L58-70(다이어그램 논리 정합) / L72("전량 22~23회 호출...0.3% 이내" `[확인]` 산술) / L73-75(키 비노출 경로 설계 타당) | AC-1(L146)은 "serviceList 전 페이지와 supportConditions 전 페이지"를 모두 수집하도록 요구하는데, L72의 산술은 serviceList 1개 오퍼레이션(22페이지)만 계산한 값이다. `python3` 재계산 결과 두 오퍼레이션을 모두 돌면 44회(0.44%)로, 명시된 "22~23회/0.3%"의 약 2배다. 일일 한도 내라는 결론 자체는 유지되나 제시된 숫자가 자신의 AC와 모순 → 근거(산술) 오류로 -15 | L72와 L217(R-8)의 "22~23회"를 "44~46회(0.44~0.46%, serviceList+supportConditions 합산)"로 정정할 것 |
| 4 | 요구사항 추적성 | 100 | 98 | L79-112(RTM 표) — R- 8종·F- 19종·S- 11종 총 38개 ID를 `grep -c`로 전수 대조, 전부 1회 이상 등장·매핑 확인. F-NQAAZL(L106)·F-KBDFAQ(L108) "제외" 명시적 처리 확인 | 제공된 정본 ID 목록 38개 전부가 RTM에 매핑돼 있고 제외 처리도 명시적이라 결함을 찾지 못함. 사소하게 S-YEGRVS/S-WVUNJJ(L105)처럼 두 ID를 한 행에 묶은 경우가 있어 완전한 1:1은 아니고 N:1(허용 범위)임 | 없음(유지) |
| 5 | 함정 대응 설계 | 100 | 95 | L118-121(기업유입, 사용자구분 필드+집합비교 규칙+표본5건 검증) / L123-126(건수불일치, 3수 대조+비승격 규칙) / L128-133(지역누락, 대체규칙+20%임계 재조정) / L135-138(인증키, 6원인 분류+check_key.py 3방식 실측) | 4개 함정 전부 필드명·판정값·검증방법이 구체적으로 명시됨. L126 "377 대 105"는 원문 미보유를 `[추정]`으로 정직하게 표기해 감점 대상 아님 | 없음(경미하게 §5.2 "377 대 105" 출처를 영상 원본에서 직접 재확인해 태그를 `[확인]`으로 승격 시도) |
| 6 | 수용기준 검증가능성 | 100 | 92 | L146-162(AC-1~AC-15 전부 When/If/무조건 형식 + 재현명령·기대출력 포함) / AC-2(L147) 실측 재현: `DATA_GO_KR_SERVICE_KEY=INVALIDKEYTEST123 python3 scripts/check_key.py` → 3방식 모두 HTTP 401 `-4`, rc=1, `grep -c INVALIDKEYTEST123`→0 — 명세와 완전 일치(독립 재현 성공) | AC-2 서술 "원인 유형과 조치 문장을 출력"은 실제 코드가 코드별 맞춤 조치 문장을 내지 않고 일반 문구("위 오류코드를 보고 원인을 판정한다")만 출력함 — 재현 절차 자체(HTTP코드·rc·키 미노출)는 통과하지만 EARS 서술과 코드 동작 사이에 사소한 표현 간극 | AC-2 서술을 실제 출력에 맞춰 "코드별 조치 문장"이 아니라 "오류 코드·메시지 표시"로 낮추거나, check_key.py에 코드별 조치 문장 분기를 추가할 것 |
| 7 | 실행 계획 구체성 | 100 | 88 | L168-186(파일표, 경로·역할·배포대상 명시) / L189-200(작업순서, 의존성·명령 명시) / L192(T1, 키 없을 때 픽스처 경로 명시) | T5(L196) 행이 "git init · GitHub 저장소 · Pages · 워크플로"를 한 셀에 뭉쳐, `.github/workflows/deploy.yml`·`refresh.yml`(L183-184에 파일로는 존재)을 실제로 작성하는 명령/단계가 작업순서표에 별도로 없음 — 파일 목록과 작업 순서 사이에 빈틈 | T5를 T5a(워크플로 yml 작성)·T5b(저장소 생성·Pages 활성화)로 분리하고 각각 명령을 적을 것 |
| 8 | 리스크·가정·데이터공백 | 100 | 78 | L206-219(R-1~R-10 표, 상태 태그·대응 명시) / L221-247(§8.1, Manyfast 미결질문 22건 처리 — `sed`+`grep`로 24줄(헤더 2줄 제외 22행) 확인) | R-8(L217) "22~23회/일"이 항목3과 동일한 산술 오류를 반복 — AC-1의 supportConditions 전량 수집을 누락한 채 일일 한도 위험을 과소평가 → -15(항목3과 별개로 이 항목 자체의 리스크 서술이 부정확하므로 독립 감점) | R-8을 "44~46회/일(0.44~0.46%)"로 정정하고 80% 경고 임계값 재계산 근거를 갱신할 것 |
| 9 | 검증·오류점검 계획 | 100 | 91 | L252-260(9.1 단계별 검증표, 통과기준·증거보관 명시) / L262-263(9.2 작성자-채점자 분리, 실제로 이 채점이 그 원칙의 실행 사례) / L265-268(9.3 키부재 검사 절차, grep 커맨드 구체적) / L270-271(9.4 롤백) | 모바일 확인(9.1 T3~T4·T6行)·롤백(9.4)·배포후 키검사(9.3) 전부 있으나, `verify_no_key.sh` 자체는 아직 파일로 존재하지 않아(저장소 `find` 결과 부재) "명령 포함" 요건이 설계 단계에 머무름 — 다만 이는 계획서 단계에서 통상적 상태라 중감점 사유로만 반영 | verify_no_key.sh를 실제로 작성해 §9.3 절차와 1:1 대응시키고 실행 로그를 EVIDENCE.md에 남길 것 |
| 10 | 비개발자 실행 가능성 | 100 | 93 | L275-283 — 7단계 한글 절차, 복붙 명령 포함, 1·2·5단계를 **굵게** 표시해 "본인만 할 수 있는 단계"(로그인·키 발급, .env 작성, Secret 등록) 명시 분리 | 6단계 `git add -A && git commit && git push`가 비개발자에게 git 사용 경험을 전제하는데 이 전제가 §10 어디에도 명시되지 않음(최초 git 설정·인증 여부 등) | 6단계 앞에 "최초 1회, git이 이미 인증돼 있어야 함(§7.2 T5에서 완료)"과 같은 전제 문장을 추가할 것 |

## 자동 반려 검사
RUBRIC.md에 별도의 자동 반려 조항은 없음(통과선 자체가 "모든 항목≥90 AND 평균≥95"). 이 통과선 기준으로 검사한 결과: 항목 1,2,3,7,8이 90 미만 → **반려 조건 충족**.

## 총점 계산 (Bash 실측)
```
scores = [75, 82, 77, 98, 95, 92, 88, 78, 91, 93]
sum = 869
평균 = 86.9
```

총점: 86.9 / 100
판정: **FAIL** (항목 미달: 1, 2, 3, 7, 8 — 90 미만 / 평균도 95 미만)

## 가장 큰 감점 3건
1. 항목 2 — L45 "행정규칙 자치법규 법령" serviceDetail 필드를 `[확인]` Swagger 근거로 표기했으나 제공된 리서치 원문 2종 어디에도 근거가 없음(`grep` 0건 실증). 근거 날조 의심.
2. 항목 1 — §1.1(L15-20)에서 P1/P2/P3(서버불필요) 전 단계를 "완성"으로 표기했으나 실제 저장소에는 `scripts/check_key.py` 외 아무 구현 파일도 없음(`find scripts docs data public tests .github` 실측). 태그 없는 허위 완료 주장.
3. 항목 3·8 — L72/L217의 "일 22~23회 호출/0.3%" 산술이 AC-1이 요구하는 serviceList+supportConditions 이중 수집을 반영하지 못해 실제값(44~46회/0.44%)의 절반만 계산됨(`python3` 재계산으로 확인). 같은 오류가 두 항목에 중복 반영되어 있음.

## 다음 라운드에서 반드시 고칠 것
1. L45 serviceDetail 추가 필드 중 "행정규칙 자치법규 법령"의 출처를 Swagger 원문 URL과 함께 재확인하거나 삭제/`[데이터공백]` 처리.
2. §1.1(L15-20) 단계 완료 표기를 실제 구현 상태(파일 존재 여부)와 일치시킬 것 — "완성" 대신 "설계 완료" 등으로 낮추거나 구현 후에만 "완성" 표기.
3. L72, L217의 API 호출 횟수 산술을 serviceList+supportConditions 합산 기준(44~46회)으로 정정.
4. §7.2 T5(L196)를 워크플로 파일 작성과 저장소/Pages 설정으로 분리해 각 단계별 명령을 명시.
5. AC-2(L147) 서술과 check_key.py 실제 출력(코드별 맞춤 조치 문장 부재) 사이 간극을 코드 보강 또는 서술 하향으로 해소.
